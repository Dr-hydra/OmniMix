# OmniMix WebUI

这是 OmniMix 后端内嵌 WebUI 的新版实现，使用 Vite + Svelte + TypeScript 构建。

## 开发

```powershell
npm install
npm run dev
```

## 构建

```powershell
npm run build
```

完整播放器构建会自动把 `dist/` 复制到 `OmniMixPlayer.Backend/wwwroot/`。
