# Kuwo Music Module

Pure C# OmniMix module for Kuwo public search results, playable URLs, lyrics, and covers.

This module is based on the public HTTP API behavior referenced from Meting-Fixed, but does not embed PHP runtime code.
